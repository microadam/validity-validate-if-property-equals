{
  "name": "@clocklimited/validity-validate-if-property-equals",
  "description": "validate the current property if another property is equal to a particular value",
  "version": "0.0.2",
  "dependencies": {},
  "main": "validator.js",
  "scripts": {
    "lint": "./node_modules/.bin/jshint .",
    "checkStyle": "./node_modules/.bin/jscs .",
    "pretest": "npm run-script lint && npm run-script checkStyle",
    "test": "./node_modules/.bin/istanbul cover ./node_modules/.bin/_mocha test",
    "posttest": "./node_modules/.bin/istanbul check-coverage && rm -rf coverage",
    "prepublish": "npm test && npm prune"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "author": "Adam Duncan <adam.jd@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "istanbul": "^0.2.7",
    "jscs": "1.3.0",
    "jshint": "^2.5.0",
    "mocha": "^1.18.2",
    "@clocklimited/validity": "1.1.3"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:clocklimited/validity-validate-if-property-equals.git"
  }
}
